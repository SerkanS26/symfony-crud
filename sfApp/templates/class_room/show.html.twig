{% extends 'base.html.twig' %}

{% block title %}ClassRoom{% endblock %}

{% block body %}
    <h1 class="text-center m-4 text-secondary font-italic">ClassRoom</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ class_room.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ class_room.name }}</td>
            </tr>
            <tr>
                <th>Location</th>
                <td>{{ class_room.location }}</td>
            </tr>
        </tbody>
    </table>

    <a class="btn btn-primary m-2" href="{{ path('class_room_index') }}">back to list</a>

    <a class="btn btn-warning m-2" href="{{ path('class_room_edit', {'id': class_room.id}) }}">edit</a>

    {{ include('class_room/_delete_form.html.twig') }}
{% endblock %}
